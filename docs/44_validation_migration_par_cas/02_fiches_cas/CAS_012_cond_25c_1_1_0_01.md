# CAS-012 Cond 25°C *1,1*0,01

## Identification
- parametre_observe : `Cond 25°C *1,1*0,01`
- nom_standard : `à confirmer`
- type_cas : `UNMAPPED_PARAMETER`
- unité source : `°C`
- criticité : `Critique`

## Données
- tables sources : `idp_2024_mesures_qualite_marche_cadre`
- volumes : `13` lignes, `13` non nulles, `0` nulles
- exemples : `21,225204 | 14,461128 | 12,251448`

## Problème
Le paramètre `Cond 25°C *1,1*0,01` n'est pas couvert par le mapping métier actuel et doit être arbitré avant toute migration.

## Analyse
Mapping observé : `à confirmer`. Unité source observée : `°C` ; unité métier dominante : `à confirmer`. Volume agrégé du cas : `13` lignes, dont `13` non nulles, `0` nulles, `0` non numériques et `0` suspectes. Plage observée dans les audits existants : min `6.101172` / max `191.99664000000001`. Types de valeurs identifiés : `DECIMAL_COMMA`. Référence externe déjà associée dans l'audit précédent : `référence bibliographique non confirmée`.

## Proposition
- action : `STAGING_ONLY`
- règles : `créer ou valider un mapping métier officiel`
- flags : `PARAMETER_UNMAPPED`
- SQL proposé (non exécuté) : `06_sql_en_attente/CAS_012_cond_25c_1_1_0_01.sql`

## Validation
- statut : `PENDING`
- validateur :
- date :

## Exécution
- script utilisé : aucun
- volume impacté : `0`

## Résultat
- succès / échec : non exécuté
- anomalies restantes : cas non traité tant que la validation humaine n’est pas fournie
